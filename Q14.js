"use strict";
let dinnerInvite = ['WASAY', 'UBAID', 'DANIYAL'];
let message = "I want to invite you to the night Dinner ";
for (let index = 0; index < dinnerInvite.length; index++) {
    console.log(message + dinnerInvite[index]);
}
